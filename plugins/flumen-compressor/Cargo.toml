[package]
name = "flumen-compressor"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
flumen-common = { path = "../../crates/flumen-common" }
flumen-engine = { path = "../../crates/flumen-engine" }
